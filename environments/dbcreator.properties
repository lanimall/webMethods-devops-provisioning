#######################################
# Default Database Configurations
#######################################

## template specific variables
repo.product=webMethods-10.1
repo.fix=Empower

dbc.host=${target.host}

########## remote provisioning ##########

## this is the secure alias we created in the CC-CREDS template
spm.credentials.alias=SECURE_ADMINISTRATOR

## these are for the remote bootstraping of SPM
bootstrap.install.dir=/opt/softwareag
bootstrap.install.installer=cc-def-10.3-fix6-${platform}.sh

# remote machine SSH port
bootstrap.ssh.port=22

## this is the SSH alias we created in the CC-CREDS template
bootstrap.credentials.alias=SSH-PRIVATE-KEY

##### databases
db.url.oracle=jdbc\:wm\:${db.type}\://${db.host}\:${db.port};SID\=${db.sid}
db.url.sqlserver=jdbc\:wm\:${db.type}\://${db.host}\:${db.port};databaseName\=${db.name}

########################################################################################################################
##############    ALL PARAMS below can be specified / overriden by env props at runtime    #############################
########################################################################################################################

target.host=

############### Database connection

# REQUIRED: Database type: oracle,sqlserver,db2
db.type=oracle

# REQUIRED: Database server hostname
db.host=

# REQUIRED: Database server port
db.port=1521

# Database name
db.name=WEBM

# Database SID (for oracle)
db.sid=XE

# Database username
db.username=

# Database password
db.password=

db.url=${db.url.oracle}

############### Database server admin connection for storage/user creation

# REQUIRED: for db storage only
db.admin.username=
db.admin.password=

# REQUIRED: for Oracle/DB2
db.tablespace.dir=
db.tablespace.data=WEBMDATA
db.tablespace.index=WEBMINDX

# wM Schemas
db.products=
db.product.version=latest
db.components=[STR]
db.component.version=latest